#include <EEPROM.h>

void setup()
{

  EEPROM.update(0, 00);
  EEPROM.update(1, 27);
  EEPROM.update(2, 14);

}

void loop()
{
  for(int i = 0; i < 3; i++){
    Serial.println(EEPROM.read(i));
  }
  delay(1000);
}
